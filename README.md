# Webapp Assignments 
โปรเจคนี้อยู่ภายใต้วิชา Web Application Development จัดทำโดย 66011511 อติวิชญ์ วัตถโกวิท

## Description  
เว็บไซต์นี้สามารถเเสดงรายละเอียดต่างๆของโดรน เเละสามารถเพิ่มเเละบันทึกข้อมูลเกี่ยวกับอุณหภูมิของโดรน

## Tech Stack  
- Frontend: React+vite
- Backend: Express
-Deploy:Vercel,Render


## Directory Structures
-  backend/ โค้ดฝั่งserver
-  frontend/ โค้ดฝั่งผู้ใช้

## 🧩 Getting Started

### ความต้องการเบื้องต้น

* **Node.js** เวอร์ชัน 18 ขึ้นไป
* **npm** (มาพร้อมกับ Node.js) สำหรับจัดการแพ็กเกจ
* ตั้งค่าไฟล์ `.env` ให้ครบในแต่ละส่วนก่อนเริ่มรันโปรเจกต์

---

### 🔧 การตั้งค่า Environment Variables

#### Backend (`backend/.env`)

```
PORT=3000
DRONE_CONFIG_URL=
DRONE_LOG_URL=
DRONE_LOG_TOKEN=
```

**อธิบายค่า:**

* `PORT` – พอร์ตเริ่มต้นของเซิร์ฟเวอร์ (ค่าเริ่มต้น 3000)
* `DRONE_CONFIG_URL` – ลิงก์สำหรับดึงข้อมูล Config ของโดรน
* `DRONE_LOG_URL` – ใช้เข้าถึงหรือบันทึก Log ของโดรน
* `DRONE_LOG_TOKEN` – โทเคนสำหรับยืนยันสิทธิ์การเข้าถึง Log API

#### Client (`client/.env`)

```
VITE_API_BASE_URL=
VITE_DRONE_ID=
```

**อธิบายค่า:**

* `VITE_API_BASE_URL` – URL ของ Backend (เช่น `http://localhost:3000`)
* `VITE_DRONE_ID` – ID ของโดรนที่ต้องการให้แสดงเริ่มต้น

---

### ⚙️ ขั้นตอนการติดตั้ง

#### 1️⃣ ติดตั้ง Backend

```bash
cd backend
npm install
```

#### 2️⃣ ติดตั้ง Client

```bash
cd client
npm install
```

---

### 🚀 การรันโปรเจกต์

เปิดเทอร์มินัลแรกเพื่อรัน **Backend**

```bash
cd backend
npm run dev
```

เซิร์ฟเวอร์จะเริ่มทำงานที่ `http://localhost:3000`

จากนั้นเปิดเทอร์มินัลอีกหนึ่งหน้าต่างเพื่อรัน **Client**

```bash
cd client
npm run dev
```

Vite Dev Server จะเริ่มที่พอร์ต `5173` (หรือพอร์ตถัดไปหาก 5173 ถูกใช้งานอยู่)

---

### 🌐 การเข้าใช้งาน

เปิดเว็บเบราว์เซอร์ แล้วไปที่
👉 `http://localhost:5173`
เพื่อเริ่มต้นใช้งานเว็บแอปพลิเคชัน
